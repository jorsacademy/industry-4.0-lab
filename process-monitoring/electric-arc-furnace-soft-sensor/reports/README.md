# Reports

`python -m src.train --config config.yaml` writes verified benchmark outputs here after the raw source has been downloaded and validated.

No benchmark number should be quoted unless it appears in committed report files produced by the benchmark workflow.
