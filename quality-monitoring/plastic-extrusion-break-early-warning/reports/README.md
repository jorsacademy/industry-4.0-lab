# Reports

Only aggregate benchmark reports are committed. Raw or transformed row-level source data are not redistributed.

`source_summary.json` records source-shape and schema diagnostics from the validated public download. Final benchmark reports are generated only after the target and leakage exclusions are verified against the source schema.
