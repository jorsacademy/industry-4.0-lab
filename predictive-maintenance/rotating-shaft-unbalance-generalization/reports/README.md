# Reports

Derived benchmark reports are written here by `python -m src.train --config config.yaml`.

The external evaluation tables are generated only after model selection is complete on the development recordings.
